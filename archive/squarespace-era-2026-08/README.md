# Archived: pre-rebuild single-page site

`index.html` and `styles.css` here are the site's content exactly as it
stood before the August 2026 rebuild (commit `9dc9b73`), which replaced the
single-page "data extraction" site with the real multi-page
momentumdatasolutions.com content (Data Management / Project Management /
HR / Managed Payroll Services, Compliance section, Resources section).

Kept for reference only — not part of the deployed site (excluded via
`.vercelignore`).
