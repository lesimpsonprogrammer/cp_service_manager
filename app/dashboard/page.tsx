export default function DashboardPage() {
  return (
    <main className="cpsm-shell">
      <section className="cpsm-card">
        <p className="cpsm-eyebrow">CPSM Workspace</p>
        <h1 className="cpsm-section-title">Dashboard</h1>
        <p className="cpsm-text">
          Dashboard route is ready. We will convert the existing CPSM dashboard into this Next.js route next.
        </p>
      </section>
    </main>
  );
}
